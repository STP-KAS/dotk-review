# For @supertypo

**This repository is not an audit.**

It is an independent Grok pass of public DOTK surfaces (site, OpenAPI, npm, a few mainnet txs) against [STP-KAS/kns-spec](https://github.com/STP-KAS/kns-spec). You did not commission it. We did not have `sil/DotkGap.sil` or `sil/DotkDeed.sil`. We did not fuzz `split` / `evict` / mass. We did not run a local wRPC prove of `covenant_id` on the deed UTXO.

You are added as a **collaborator (push)** so you can check:

1. Facts that are wrong (lineage, fees, seats, cards, compiler version).
2. Overclaims we attributed to the site/X that you would word differently.
3. The missing GitHub `github.com/supertypo/dotk` (JSON-LD + npm `repository` still 404 on 15 Sep 2026).

Sister mix (also inviting you): https://github.com/STP-KAS/kns-dotk

Please comment, open an issue, or PR. A correction from you is better than us guessing.

Contact we used from your OpenAPI: `suprtypo@pm.me` (spelling as published).
